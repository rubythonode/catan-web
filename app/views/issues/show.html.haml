= render 'issue_header'

.bs-component
  %section#issue-posts
    .container

      = render 'post_type_filter'

      .row.masonry-container
        - if @issue.is_all?
          = render @postables, show_issue: true
        - else
          = render @postables
