- current_params = params.except(:page, :f, :action, :controller)
- if local_assigns[:issue]
  - recommend_position = postables.count < 4 ? postables.count - 1: 3
- else
  - recommend_position = -1

- yesterday_count = local_assigns[:issue].posts.for_list.yesterday.count
- if params[:f] == 'hottest' and yesterday_count > 0
  .bg-warning(style='padding: 10px; color: #777')
    하루동안 새로운 글이 #{yesterday_count}개 올라왔습니다.
    = link_to url_for(current_params.merge(f: :recent)), style: 'color: #333' do
      확인하기
- postables.each_with_index do |postable, index|
  = render postable, show_issue: local_assigns[:show_issue]
  - if index == recommend_position
    = render 'recommends', issue: local_assigns[:issue]
