.panel-footer
  .post-card__buttons
    = render 'likes/like_line', post: post
    %span.post-card__buttons__comments
      = link_to post.specific do
        %i.fa.fa-comment-o
        = post.comments_count

  = render 'comments/newest_comment', postable: post
