- current_params = params.except(:t, :action, :controller)
%ul.nav.nav-pills{role: 'tablist', style: 'margin-bottom: 20px'}
  %li.active{role: 'presentation'}= link_to '모두', url_for(current_params)
  %li{role: 'presentation'}= link_to '뉴스컬럼만', url_for(current_params.merge(t: :article))
  %li{role: 'presentation'}= link_to '주장만', url_for(current_params.merge(t: :opinion))
