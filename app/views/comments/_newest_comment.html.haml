- newest_comment = postable.comments.past_day.newest
- if newest_comment
  .panel-footer
    .newest_comment
      .byline
        = byline newest_comment.user
      %i.fa.fa-clock-o
      = date_f newest_comment.created_at
      = simple_format truncate(newest_comment.body)
