.bs-component
  %section#comments
    .container
      %ul.list-group
        %li.list-group-item
          = form_for [postable.acting_as, postable.comments.build] do |f|
            .form-group
              = f.text_area :body
            = f.submit '저장', class: 'btn btn-primary'
        = render postable.comments.persisted.recent

