$('#<%= dom_id(@issue) %>-watch').html("<%= escape_javascript(render 'watches/watch', issue: @issue) %>");
<% if current_user.watched_non_default_issues? %>
$('#btn-continue').removeClass('disabled');
<% end %>